proc assert_(condition: bool, file: *[]byte, line: uint) {
    if !condition {
        print!("{}:{}: Assertion failed!\n", file, line);
        panic();
    };
}

proc panic() {
    var _: uint = @syscall1(60, 1);
}
