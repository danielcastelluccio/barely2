//@out: a

type A : enum {
    A,
    B
}

proc main() {
    var a: A = A;
    if a == A {
        var _: usize = @syscall3(1, 1, "a", 1);
    };
    if a == B {
        var _: usize = @syscall3(1, 1, "a", 1);
    };
}
