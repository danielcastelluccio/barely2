//@out: a

type A : enum {
    A,
    B
};

proc main : () {
    var a: A = A::A;
    if a == A::A {
        var _: usize = @syscall3(1, 1, "a", 1);
    };
    if a == A::B {
        var _: usize = @syscall3(1, 1, "a", 1);
    };
};
